import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  input: {
    width: '30%',
    fontSize: 50,
    alignItems: 'center',
    backgroundColor: '#fff',
    borderWidth: 1
  }
});