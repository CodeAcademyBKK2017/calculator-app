import {
  StyleSheet
} from 'react-native';

const styles = StyleSheet.create({
  container: {
    // backgroundColor: 'red'
  },
  title: {
    fontWeight: 'bold',
    fontSize: 80,
    color: 'black'
  }
});

export default styles;
